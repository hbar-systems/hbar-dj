# hbar.dj base deps (created 2026-06-16). Stems are a separate heavy extra.
# No anthropic / no API key — the voice brain is a local parser in static/app.js.
fastapi
uvicorn[standard]
python-multipart
httpx          # proxy to a separate music box when HBAR_DJ_BACKEND_URL is set
librosa
soundfile
numpy
